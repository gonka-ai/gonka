server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml

clients:
  - url: http://loki:3100/loki/api/v1/push

scrape_configs:
  - job_name: join-containers
    docker_sd_configs:
      - host: unix:///var/run/docker.sock
        refresh_interval: 5s
    pipeline_stages:
      - docker: {}
      # Parse JSON body emitted by slog (decentralized-api and devshard).
      # Non-JSON lines (e.g. cosmos node output) are silently skipped.
      # Extract correlation fields but promote only low-cardinality labels.
      - json:
          expressions:
            level: level
            service: service
            stage: stage
            where: where
            trace_id: trace_id
            span_id: span_id
            request_id: request_id
      # Promote low-cardinality fields to Loki labels for efficient filtering.
      # High-cardinality fields (trace_id, request_id, nonce, …) stay in the log
      # body; Grafana derivedFields / `| json` extract them at read time.
      - labels:
          service:
          level:
          stage:
    relabel_configs:
      - source_labels: [__meta_docker_container_name]
        regex: '/(.*)'
        target_label: container
      - source_labels: [__meta_docker_container_label_com_docker_compose_service]
        target_label: compose_service
      - source_labels: [__meta_docker_container_label_com_docker_compose_project]
        target_label: compose_project
      - source_labels: [__meta_docker_container_log_stream]
        target_label: stream
      - target_label: environment
        replacement: join
      - source_labels: [__meta_docker_container_id]
        target_label: __path__
        replacement: /var/lib/docker/containers/$1/$1-json.log