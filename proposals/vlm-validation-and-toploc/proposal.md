# Validation Mechanism Expansion

## Motivation

In decentralized inference networks, providers have economic incentive to reduce computational costs through various deviations:

- Using quantized models (int4) while claiming higher precision (fp8)
- Substituting a smaller/faster model
- Returning cached or fabricated results

Gonka already employs validation of token sequences generated by distributed nodes serving large language models (LLMs), which enables the detection of fraudulent behavior and dishonest model hosting. A logical next step is to improve and extend the validation system.

## Key Improvements

1. **Add a separate validation endpoint** to avoid mixing validation code and core vLLM code. Transfer naive text generation validation to VLM.

2. **Add TOPLOC algorithm** in order to reduce artifact size. The goal is to study the applicability of TopLoC: will using this algorithm improve validation reliability without increasing storage costs? This involves finding the optimal approach and testing on synthetic and real data.

3. **Extend this validation framework to additional model classes**, in particular Visual Language models. Deploy VLM, run it on the image understanding tasks benchmark, collect artifacts, perform validation, and calculate thresholds. This task requires a series of experiments and is computationally expensive, but the task involves little uncertainty.

## TOPLOC Overview

[TOPLOC](https://arxiv.org/pdf/2501.16007) is a novel verification method designed to ensure that AI inference providers are using the specific models and prompts they claim without unauthorized changes. By utilizing a locality-sensitive hashing scheme, the system tracks the most significant intermediate activations to detect modifications in model weights, prompts, or numerical precision. A key innovation is the use of polynomial encoding, reducing necessary storage size. This approach remains robust against hardware non-determinism and GPU-specific variations that typically cause minor numerical discrepancies. Empirical testing confirms the method achieves 100% accuracy in identifying tampering while maintaining validation speeds faster than the original inference.

We plan to primarily add a separate endpoint for validation (in the current implementation, enforced tokens are generated by `/COMPLETION` endpoint).

We plan to implement TOPLOC algorithm without affecting other functionality and without influencing the performance of the platform core. During the development process, we plan to adapt the approach from the article described above to the inference of visual language models and integrate the necessary tools into the inference engine.

## Scope

- Endpoint for validation
- TOPLOC Algorithm for textual endpoints
- VLMs ([Qwen-VL](https://huggingface.co/Qwen/Qwen3-VL-235B-A22B-Thinking-FP8)) support with "naive" (direct comparison) validation approach

### Hardware Matrix

- A100 / H100 / H100 with PP=2 / B200 GPUs

### Validation Scenarios

- API tests for the new validation endpoint
- Do image-related tasks on the one machine, save artifacts and validate them on the other machine. Include threshold calibration, using "honest" and "fraud" scenarios.
- Compare TOPLOC validation for textual tasks with existing one

## Technical Steps

1. Add validation endpoint to vLLM
2. Create a naive artifact validation baseline for VLMs
3. Measure the size of artifact and time to validate and compare to TOPLOC later (for text generation tasks)
4. Prepare Top-Loc based validation code for VLMs
5. Local tests
6. Threshold calibration for TOPLOC
7. PR preparation

## Deliverables

- Upgrade PR (compatibility fixes, tests, Docker images)
- MLNode PR / MLNode release with updated dependencies
- Recalibrated thresholds with calibration report (statistical analysis, notebooks, raw data)
- Migration guide for MLNode operators

## Timeline

| Week | Milestone |
|------|-----------|
| Week 1 | New validation endpoint |
| Week 2-3 | TOPLOC algorithm implementation |
| Week 4 | VLMs support implementation |
| Week 5 | Review, Testing and Experiments |

**Target completion:** Mid of March 2026

## Budget

**10,000 GNK**

## Who We Are

A small team of engineers (aka MIL-Team) specializing in LLM inference and production ML systems, with hands-on experience in vLLM internals.
