version: "3.7"

services:
  node:
    command: inferenced start
    container_name: node
    image: inferenced
    volumes:
      - /Users/dima/.inference:/root/.inference
  api:
    container_name: api
    image: decentralized-api
    depends_on:
      - node
