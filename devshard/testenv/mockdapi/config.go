package mockdapi

// TODO(phase-5): Config.
//
//   type Config struct {
//       HeightSyncURL    string        // http URL of the height-sync container
//       ValidatorPubHex  string        // pinned secp256k1 validator pubkey (hex)
//       ChainID          string        // expected chain id on every header
//       ResubscribeAfter time.Duration // SSE reconnect backoff cap
//   }
