package types

const (
	// DefaultMaxVoters is the default number of voters to ping for fallback verification.
	DefaultMaxVoters uint32 = 5
	// DefaultMaxVoters is the default maximum number of voters that may be skipped during sampling.
	DefaultMaxSkips uint32 = 1
	// DefaultMaxVoters is the default number of voters to sample for fallback verification.
	DefaultMaxVotersToSample uint32 = 10
)
