syntax = "proto3";
package inference.inference;

import "inference/inference/inference.proto";
option go_package = "github.com/productscience/inference/x/inference/types";

message DeveloperStatsByEpoch {
  uint64 epoch_id = 1;
  map<string, InferenceStats> inferences = 2;
}

message InferenceStats {
  string inference_id = 1;
  InferenceStatus status = 2;
  uint64 ai_tokens_used = 3;
}

message DeveloperStatsByTime {
  uint64 epoch_id = 2;
  int64 timestamp = 3;
  InferenceStats inference = 4;
}