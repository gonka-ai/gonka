name: Sanity Tests

on:
  workflow_dispatch:
  issue_comment:
    types: [created]

jobs:
  check-comment:
    runs-on: ubuntu-latest
    if: ${{ github.event_name == 'issue_comment' && github.event.issue.pull_request && contains(github.event.comment.body, '/run-sanity') }}
    outputs:
      should_run: 'true'
    steps:
      - name: Check comment
        run: echo "Comment contains /run-sanity, triggering sanity tests"

  sanity-tests:
    needs: check-comment
    if: ${{ github.event_name == 'workflow_dispatch' || (github.event_name == 'issue_comment' && needs.check-comment.outputs.should_run == 'true') }}
    uses: ./.github/workflows/test-workflow.yml
    with:
      test_name: "Sanity Tests"
      test_command: "run-sanity"