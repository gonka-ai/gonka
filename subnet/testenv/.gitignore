# Generated by make gen-compose — regenerate freely, but commit config.yaml
docker-compose.yml

# Local binaries built during development (go build ./cmd/mockserver etc.)
# These match the cmd/ subdirectory names but are compiled executables, not source.
bin/mockserver
bin/subnethost
bin/gencompose
./config.yaml

# air build artefacts (live-reload tmp binaries)
/tmp/air-subnethost/
/tmp/air-mockserver/

# db volumes
db